# Render.com Build Configuration
services:
  - type: web
    name: virtual-mirror-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_async:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
